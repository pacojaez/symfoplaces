function toggleDemo() {

    halfmoon.toggleDarkMode();

    // if (halfmoon.readCookie("halfmoon_preferredMode")) {
    //     if (halfmoon.readCookie("halfmoon_preferredMode") == "light-mode") { // Light mode is preferred
    //         sun.classList.add('d-none');
    //         moon.classList.remove('d-none');
    //     } else if (halfmoon.readCookie("halfmoon_preferredMode") == "dark-mode") { // Dark mode is preferred
    //         sun.classList.add('d-none');
    //         moon.classList.remove('d-none');
    //     }
    // }

}