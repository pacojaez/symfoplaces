<div class="d-flex text-center m-20 p-5">
	<div class="container-fluid m-10">
		{{ form_start(formulario)}}
		<div class="row p-5 m-5 border">
		{# <div class="d-flex flex-row justify-content-around align-items-center"> #}
			<div class="form-group col-xl-6">{{ form_row(formulario.place)}}</div>
			<div class="form-group col-xl-6">{{ form_row(formulario.SEARCH)}}</div>
			{# {{ form_rest(formulario)}} #}
		</div>
		{{ form_end(formulario) }}
	</div>
</div>
